{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy-fb": "firebase target:apply hosting pribadisql nuxt-farmly && .output/server && rd /s /q node_modules && npm i && cd .. && firebase deploy"
  },
  "devDependencies": {
    "@types/node": "^18",
    "autoprefixer": "^10.4.14",
    "firebase-admin": "^11.7.0",
    "firebase-functions": "^4.3.1",
    "firebase-functions-test": "^3.1.0",
    "nuxt": "^3.4.2",
    "postcss": "^8.4.23",
    "tailwindcss": "^3.3.1"
  }
}
